name: Publish Any Commit

on: [push, pull_request]

permissions: {}

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm --filter './packages/*' build

      - name: Publish to pkg.pr.new
        run: pnpm dlx pkg-pr-new publish --pnpm --compact './packages/*' --no-template